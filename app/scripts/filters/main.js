/**
 * Define here the filters that are global to the app.
 *
 * You can break them down into modules. We did that with an example filter.
 */
define([ //
    './exampleFilter' //
    // Require your files here
], function () {
    // Using EZWeb module auto-loading
});
